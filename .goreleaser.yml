version: 2
project_name: beacon
before:
  hooks: [go mod tidy]
builds:
  - id: beacon
    main: ./cmd/beacon
    binary: beacon
    ldflags: ["-s", "-w", "-X main.version={{.Version}}"]
    goos: [linux, darwin]
    goarch: [amd64, arm64]
  - id: beacon-server
    main: ./cmd/beacon-server
    binary: beacon-server
    ldflags: ["-s", "-w", "-X main.version={{.Version}}"]
    goos: [linux, darwin]
    goarch: [amd64, arm64]
  - id: beacon-agent
    main: ./cmd/beacon-agent
    binary: beacon-agent
    ldflags: ["-s", "-w", "-X main.version={{.Version}}"]
    goos: [linux, darwin]
    goarch: [amd64, arm64]
dockers:
  - ids: [beacon-server]
    image_templates: ["ghcr.io/sanskarpan/beacon:{{.Version}}", "ghcr.io/sanskarpan/beacon:latest"]
    dockerfile: Dockerfile.release
checksum: { name_template: checksums.txt }
brews:
  - name: beacon
    repository:
      owner: sanskarpan
      name: homebrew-tap
    homepage: https://github.com/sanskarpan/beacon
    description: Consul-class service discovery — gossip + Raft catalog, DNS/HTTP/gRPC/xDS
    skip_upload: auto
sboms:
  - artifacts: archive
changelog:
  sort: asc
  filters: { exclude: [^docs:, ^chore:, ^test:] }
